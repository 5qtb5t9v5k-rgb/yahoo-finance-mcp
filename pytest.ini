[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    live: hits Yahoo for real — opt-in via YAHOO_MCP_LIVE=1 (skipped by default)
addopts = -ra --strict-markers
